@model IEnumerable<Punto_de_ventaMVC.Models.Venta>

@{
    ViewData["Title"] = "Listado de Ventas";
}

<link rel="stylesheet" href="~/css/styles.css" />

<h1>@ViewData["Title"]</h1>

<a href="@Url.Action("Create", "Ventas")" class="btn btn-primary my-3">Nueva Venta</a>

<table class="table table-striped table-bordered table-hover dataTableJS">
    <thead>
        <tr>
            <th hidden>ID</th>
            <th>Numero</th>
            <th>Cliente</th>
            <th>Usuario</th>
            <th>Fecha</th>
            <th>Total</th>
            <th>SubTotoal</th>
            <th>ISV</th>
            <th>descuento</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var venta in Model)
        {
            <tr>
                <td hidden>@venta.id_factura</td>
                <td>@venta.numero</td>
                <td>@venta.Cliente.nombre, @venta.Cliente.apellido</td>
                <td>@venta.usuario</td>
                <td>@venta.fecha_facturacion</td>
                <td>@venta.total</td>
                <td>@venta.subtotal</td>
                <td>@venta.isv</td>
                <td>@venta.descuento</td>
                <td>
                    <a href="@Url.Action("Edit", "Ventas", new { id = @venta.id_factura })" class="btn btn-warning text-white"><i class="bi bi-pencil-square"></i></a>
                    <a href="@Url.Action("Details", "Ventas", new { id = @venta.id_factura })" class="btn btn-info text-white"><i class="bi bi-search"></i></a>
                    <a href="@Url.Action("Delete", "Ventas", new { id = @venta.id_factura })" class="btn btn-danger text-white"><i class="bi bi-trash3"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>